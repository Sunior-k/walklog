<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.walklog" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorAccent">@color/walklog_primary</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="colorAccent">@color/walklog_primary</item>
        <item name="colorPrimary">@color/walklog_primary</item>
        <item name="colorPrimaryDark">@color/walklog_primary_dark</item>
    </style>

    <style name="Theme.WalkLog.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/walklog_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/walklog_primary</item>
        <item name="postSplashScreenTheme">@style/Theme.walklog</item>
    </style>

    <style
        name="Widget.Walklog.BottomNavigationView.ActiveIndicator"
        parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>

</resources>
