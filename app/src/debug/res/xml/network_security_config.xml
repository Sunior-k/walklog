<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug 빌드 전용 오버라이드 (main의 network_security_config.xml을 대체).
    리워드 스토어(React Native) Metro 개발 서버(10.0.2.2:8081, localhost:8081) 접속을 위해
    해당 호스트에 한해서만 평문(HTTP) 트래픽을 허용 — 그 외 모든 도메인은 main과 동일하게
    cleartext를 차단해, 디버그 빌드라도 공용 네트워크에서의 MITM 노출 범위를 최소화한다.
-->
<network-security-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>

</network-security-config>
